import React from "react";
import UsersEditCard from "../components/userEditCard.js";

const UserList = (props) => {
  return (
    <div className="list">
      <UsersEditCard />
    </div>
  );
};

export default UserList;
